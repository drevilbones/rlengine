#ifndef RENDERER_H
#define RENDERER_H

#include <stdio.h>
#include <SDL2/SDL.h>


class Renderer
{
    public:
        Renderer();
        virtual ~Renderer();
        
        void render();

    protected:

    private:
        SDL_Window* window;
        SDL_Surface* screenSurface;
};

#endif // RENDERER_H
